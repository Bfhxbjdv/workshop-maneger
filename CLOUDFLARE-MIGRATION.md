# انتقال الموقع إلى Cloudflare

## ما تم تجهيزه

يدعم الموقع الآن تخزين ملفات الطلبات الجديدة في Cloudflare R2 عند إضافة متغيرات
البيئة أدناه. تبقى الملفات والسجلات القديمة قابلة للقراءة من التخزين المحلي أو
Google Drive، لذلك لا يوجد انقطاع أثناء الانتقال.

## ربط R2

1. أنشئ Bucket باسم `workshop-files` من صفحة R2 في Cloudflare.
2. أنشئ R2 API Token بصلاحيات قراءة وكتابة وحذف لهذا الـBucket فقط.
3. أضف القيم الموجودة في `.env.cloudflare.example` إلى أسرار خدمة Render.
4. أعد نشر الموقع. عندها تحفظ ملفات الطلبات الجديدة في R2 تلقائياً.

لا تضع مفاتيح R2 في Git أو داخل ملفات المشروع المنشورة.

## المرحلة التالية

بعد التأكد من رفع وتنزيل ملفات جديدة بنجاح من R2، ننقل قاعدة البيانات من sql.js
المحلية إلى PostgreSQL ثم نجهز نسخة Workers تجريبية قبل تغيير النطاق.
